import { Mongo } from 'meteor/mongo';

/*export const Groups = new Mongo.Collection('groups');

Lists.schema = new Groups({
  id: {type:  Number},
  name: {type: String},
  description: {type: String};
});

Groups.helpers({
  user() {
    return User.find({});
  }
});*/
